# 📖 算法专项训练笔记

**时间：** 2026-04-20 02:27 开始  
**专项：** 算法与数据结构  
**目标：** LeetCode 5 题 (3 简单 + 2 中等)

---

## 题目 1：两数之和 (Two Sum)

**难度：** 简单  
**开始时间：** 02:27

### 题目描述

给定一个整数数组 nums 和一个整数目标值 target，在该数组中找出和为目标值的两个整数，返回它们的数组下标。

### 解题思路

使用哈希表存储已遍历的数字及其索引，对于每个数字，检查 target - num 是否已在哈希表中。

### 代码实现

```javascript
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
function twoSum(nums, target) {
  const map = new Map();

  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }

  return [];
}

// 测试用例
console.log(twoSum([2, 7, 11, 15], 9)); // [0, 1]
console.log(twoSum([3, 2, 4], 6)); // [1, 2]
console.log(twoSum([3, 3], 6)); // [0, 1]
```

### 复杂度分析

- 时间复杂度：O(n) - 只需遍历一次数组
- 空间复杂度：O(n) - 哈希表存储 n 个元素

### 完成时间：\*\* 02:32 (5 分钟)

---

## 题目 2：有效的括号 (Valid Parentheses)

**难度：** 简单  
**开始时间：** 02:32

### 题目描述

给定一个只包括 '(', ')', '{', '}', '[', ']' 的字符串，判断字符串是否有效。

### 解题思路

使用栈：遇到左括号入栈，遇到右括号检查栈顶是否匹配，匹配则出栈，最后检查栈是否为空。

### 代码实现

```javascript
/**
 * @param {string} s
 * @return {boolean}
 */
function isValid(s) {
  const stack = [];
  const map = {
    ")": "(",
    "}": "{",
    "]": "[",
  };

  for (const char of s) {
    if (char in map) {
      const top = stack.pop() || "#";
      if (map[char] !== top) {
        return false;
      }
    } else {
      stack.push(char);
    }
  }

  return stack.length === 0;
}

// 测试用例
console.log(isValid("()")); // true
console.log(isValid("()[]{}")); // true
console.log(isValid("(]")); // false
console.log(isValid("([)]")); // false
console.log(isValid("{[]}")); // true
```

### 复杂度分析

- 时间复杂度：O(n) - 遍历字符串一次
- 空间复杂度：O(n) - 栈的最大深度

### 完成时间：\*\* 02:38 (6 分钟)

---

## 题目 3：合并两个有序数组 (Merge Sorted Array)

**难度：** 简单  
**开始时间：** 02:38

### 题目描述

给你两个按非递减顺序排列的整数数组 nums1 和 nums2，另有两个整数 m 和 n，分别表示 nums1 和 nums2 中的元素数目。合并 nums2 到 nums1 中，使合并后的数组同样按非递减顺序排列。

### 解题思路

从后向前双指针：比较两个数组末尾元素，将较大的放到 nums1 末尾，避免覆盖未处理的元素。

### 代码实现

```javascript
/**
 * @param {number[]} nums1
 * @param {number} m
 * @param {number[]} nums2
 * @param {number} n
 * @return {void}
 */
function merge(nums1, m, nums2, n) {
  let p1 = m - 1;
  let p2 = n - 1;
  let p = m + n - 1;

  while (p2 >= 0) {
    if (p1 >= 0 && nums1[p1] > nums2[p2]) {
      nums1[p] = nums1[p1];
      p1--;
    } else {
      nums1[p] = nums2[p2];
      p2--;
    }
    p--;
  }
}

// 测试用例
let nums1 = [1, 2, 3, 0, 0, 0];
merge(nums1, 3, [2, 5, 6], 3);
console.log(nums1); // [1, 2, 2, 3, 5, 6]

nums1 = [1];
merge(nums1, 1, [], 0);
console.log(nums1); // [1]

nums1 = [0];
merge(nums1, 0, [1], 1);
console.log(nums1); // [1]
```

### 复杂度分析

- 时间复杂度：O(m + n)
- 空间复杂度：O(1)

### 完成时间：\*\* 02:45 (7 分钟)

---

## 题目 4：三数之和 (3Sum)

**难度：** 中等  
**开始时间：** 02:45

### 题目描述

给你一个整数数组 nums，判断是否存在三元组 [nums[i], nums[j], nums[k]] 满足 i != j、i != k 且 j != k，同时还满足 nums[i] + nums[j] + nums[k] == 0。返回所有和为 0 且不重复的三元组。

### 解题思路

排序 + 双指针：

1. 先排序
2. 固定一个数，用双指针在剩余数组中找两数之和等于目标值
3. 注意去重

### 代码实现

```javascript
/**
 * @param {number[]} nums
 * @return {number[][]}
 */
function threeSum(nums) {
  const result = [];
  nums.sort((a, b) => a - b);

  for (let i = 0; i < nums.length - 2; i++) {
    // 跳过重复的固定数
    if (i > 0 && nums[i] === nums[i - 1]) continue;

    // 如果当前数大于 0，后面的数也都大于 0，不可能和为 0
    if (nums[i] > 0) break;

    let left = i + 1;
    let right = nums.length - 1;

    while (left < right) {
      const sum = nums[i] + nums[left] + nums[right];

      if (sum === 0) {
        result.push([nums[i], nums[left], nums[right]]);

        // 跳过重复的左指针
        while (left < right && nums[left] === nums[left + 1]) left++;
        // 跳过重复的右指针
        while (left < right && nums[right] === nums[right - 1]) right--;

        left++;
        right--;
      } else if (sum < 0) {
        left++;
      } else {
        right--;
      }
    }
  }

  return result;
}

// 测试用例
console.log(threeSum([-1, 0, 1, 2, -1, -4])); // [[-1, -1, 2], [-1, 0, 1]]
console.log(threeSum([0, 1, 1])); // []
console.log(threeSum([0, 0, 0])); // [[0, 0, 0]]
```

### 复杂度分析

- 时间复杂度：O(n²) - 排序 O(nlogn) + 双层循环 O(n²)
- 空间复杂度：O(1) - 不计返回结果

### 完成时间：\*\* 02:55 (10 分钟)

---

## 题目 5：无重复字符的最长子串 (Longest Substring Without Repeating Characters)

**难度：** 中等  
**开始时间：** 02:55

### 题目描述

给定一个字符串 s，请你找出其中不含有重复字符的最长子串的长度。

### 解题思路

滑动窗口 + 哈希表：

- 使用左右指针维护一个滑动窗口
- 用 Set 记录窗口中的字符
- 遇到重复字符时，移动左指针直到没有重复

### 代码实现

```javascript
/**
 * @param {string} s
 * @return {number}
 */
function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left = 0;
  let maxLength = 0;

  for (let right = 0; right < s.length; right++) {
    // 如果字符重复，移动左指针直到不重复
    while (set.has(s[right])) {
      set.delete(s[left]);
      left++;
    }

    set.add(s[right]);
    maxLength = Math.max(maxLength, right - left + 1);
  }

  return maxLength;
}

// 测试用例
console.log(lengthOfLongestSubstring("abcabcbb")); // 3 ("abc")
console.log(lengthOfLongestSubstring("bbbbb")); // 1 ("b")
console.log(lengthOfLongestSubstring("pwwkew")); // 3 ("wke")
console.log(lengthOfLongestSubstring("")); // 0
console.log(lengthOfLongestSubstring("abcdef")); // 6
```

### 复杂度分析

- 时间复杂度：O(n) - 左右指针各遍历一次
- 空间复杂度：O(min(m,n)) - m 为字符集大小

### 完成时间：\*\* 03:05 (10 分钟)

---

## 📊 算法专项总结

| 题目                 | 难度 | 耗时    | 状态    |
| -------------------- | ---- | ------- | ------- |
| 两数之和             | 简单 | 5 分钟  | ✅ 完成 |
| 有效的括号           | 简单 | 6 分钟  | ✅ 完成 |
| 合并两个有序数组     | 简单 | 7 分钟  | ✅ 完成 |
| 三数之和             | 中等 | 10 分钟 | ✅ 完成 |
| 无重复字符的最长子串 | 中等 | 10 分钟 | ✅ 完成 |

**总耗时：** 38 分钟  
**代码行数：** 约 200 行  
**完成度：** 5/5 (100%)

---

## 💡 收获与反思

### 收获

1. 哈希表在查找问题中非常高效
2. 双指针技巧适用于有序数组和字符串问题
3. 滑动窗口是处理子串/子数组问题的利器
4. 注意边界条件和去重逻辑

### 待加强

1. 中等题目的思路转换需要更快
2. 复杂度分析要更熟练

---

_下一专项：03:00 JavaScript 深度_
