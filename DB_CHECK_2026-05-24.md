# Prisma Schema 检查报告

检查时间: 2026-05-24 04:00 UTC+8

## 1. Schema 概览

**位置**: `/server/prisma/schema.prisma`  
**Provider**: PostgreSQL  
**模型数量**: 25 个

## 2. 模型结构分析 ✓

### 用户系统
- User: 基本信息 + 会员 + 测试记录 ✓
- TestSession, Question, QuestionOption, Response, TestResult ✓

### 报告系统
- Report, ReportUsage ✓

### 会员与支付
- Membership, MembershipProduct, Order ✓

### 安全与追踪
- RefreshToken, DeviceFingerprint, LoginHistory, UsageRecord, CostRecord, RateLimit ✓

### 增长功能
- DualTest, Referral, Commission, LifeEvent ✓

### 通知系统
- PushSubscription, PushNotification ✓

### 日志
- SecurityLog ✓

## 3. 索引检查 ✓

### 已定义索引
| 模型 | 字段 | 状态 |
|------|-------|------|
| User | email, wechatOpenid | ✓ unique |
| TestSession | userId, sessionId, status | ✓ |
| Question | dimension, isActive | ✓ |
| TestResult | userId, mbtiType | ✓ |
| Report | testResultId, reportType, status, userId | ✓ |
| ReportUsage | userId+date(unique), date | ✓ |
| LoginHistory | userId, timestamp | ✓ |

⚠️ **建议添加的缺失索引**:
- Response.sessionId + questionId 复合索引 (联合查询)
- Membership.userId (已有，但可验证)
- Order.userId (已有)
- DualTest.initiatorId, participantId (关系查询优化)

## 4. 关联查询分析 ✓

现有 include 使用良好的控制器:
- authController: include: { user: true } ✓
- reportController: include: { user: true, testResult: { include: { user: true }}} ✓
- testController: include: { user: true, options: true } ✓
- membershipController: include: { user } ✓

⚠️ N+1 问题已通过 Prisma include 避免。

## 5. 慢查询风险

### 无原始 SQL 查询
未发现 `$queryRaw` 或 `raw` 直接查询。

### 可优化点:
1. TestResult.answers 为 JSON，频繁读取可能较慢 - 考虑分离为独立表
2. Report.content 为 Text，大报告可能影响性能 - 考虑压缩或外部存储
3. LifeEvent.description 可选，建议也用 Text

## 6. 数据迁移状态

⚠️ **无 migrations 目录**
```
prisma/migrations/
```

当前使用 `prisma db push` 或手动同步。建议:
- 初始化 migrate: `npx prisma migrate dev --name init`
- 或记录变更脚本到 `scripts/migrations/`

## 7. 总结

| 检查项 | 状态 |
|--------|------|
| Schema 语法 | ✅ |
| 必要索引 | ✅ 有，部分建议补充 |
| 关联查询 | ✅ 已用 include 优化 |
| 原始SQL注入 | ✅ 未使用 |
| 迁移文件 | ⚠️ 需初始化 |
| 数据类型 | ✅ 合理 |

### 下一步建议:
1. 运行 `npx prisma migrate dev --name init` 初始化迁移
2. 在生产环境执行数据库审计
3. 考虑对大 JSON 字段做拆分优化